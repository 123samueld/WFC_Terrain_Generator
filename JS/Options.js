//Options

const options = {
    play: true,
    showGrid: true,
    scrollSpeed: 1,
    showHotkeyReminders: false,
    visualiseTerrainGenerationProcess: false,
    volume: 1,     // Volume level (0-1)
    displayGrid: true,  // Display grid toggle
    disableMapScrolling: false  // Disable map scrolling when modals are open
};

export { options };